{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --omit=dev"
  },
  "deploy": {
    "startCommand": "npm start"
  },
  "volumes": {
    "/data": {
      "name": "db-data"
    }
  }
}
